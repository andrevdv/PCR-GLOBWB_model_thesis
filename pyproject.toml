[build-system]
requires = [
    "hatchling",
]
build-backend = "hatchling.build"

[project]
name = "bmi-pcrglobwb"
version = "0.1.0"
description = "Python 3 port of PCR-GLOBWB + BMI"
authors = [
    { name = "A.B. van der Veen" }
]
dependencies = [
    "cython",
    "pydantic"
]

[tool.hatch.build]
packages = ["model"]

